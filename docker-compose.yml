version: "3.9"

services:
  # NVIDIA NIM LLM — Nemotron Nano 9B v2 (DGX Spark / GB10 optimized)
  nim-llm:
    image: nvcr.io/nim/nvidia/nvidia-nemotron-nano-9b-v2-dgx-spark:1.0.0-variant
    ports:
      - "8000:8000"
    environment:
      - NGC_API_KEY=${NGC_API_KEY}
    runtime: nvidia
    shm_size: "16gb"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/v1/health/live"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 300s
    profiles:
      - nim

  # RAG Chatbot Web UI
  chatbot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - ./papers:/app/papers:ro
      - ./rag/data:/app/rag/data
      - ./rag/config:/app/rag/config
    environment:
      - NVIDIA_API_KEY=${NVIDIA_API_KEY:-}
      - NVIDIA_VISIBLE_DEVICES=all
    depends_on:
      nim-llm:
        condition: service_healthy
        required: false
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    profiles:
      - app

  # Standalone chatbot (no NIM, uses local model or cloud API)
  chatbot-standalone:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - ./papers:/app/papers:ro
      - ./rag/data:/app/rag/data
      - ./rag/config:/app/rag/config
    environment:
      - NVIDIA_API_KEY=${NVIDIA_API_KEY:-}
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    profiles:
      - standalone
